# Apache Sedona boundary

Apache Sedona is disabled until an approved Spark cluster, governed warehouse, catalog, and secret-managed object-store credentials are configured. The reporting service accepts only Nigeria (NG), Kenya (KE), and South Africa (ZA) jurisdiction-level cohorts of at least ten records and rejects raw coordinates or identifying fields before any Sedona job may run.
