# OpenSearch Security plugin roles_mapping.yml
# Replace the subject only with the exact subject emitted by the worker
# client certificate. Do not use a wildcard or a broad OU mapping.
umoja_retention_delete_worker:
  reserved: false
  hidden: false
  backend_roles: []
  hosts: []
  users:
    - "CN=umoja-retention-delete-worker,OU=Platform Security,O=UmojaFlowOS,L=Lagos,ST=Lagos,C=NG"
