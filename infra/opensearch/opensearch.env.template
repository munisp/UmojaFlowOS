# Disabled until a private, TLS-enabled OpenSearch deployment is approved.
# Search is a redacted audit/case projection; it is never a system of record.
UMOJA_OPENSEARCH_ENABLED=false
UMOJA_OPENSEARCH_URL=
UMOJA_OPENSEARCH_BEARER_TOKEN_SECRET_REF=REPLACE_WITH_SECRET_REFERENCE
UMOJA_OPENSEARCH_TLS_REQUIRED=true
UMOJA_OPENSEARCH_TLS_VERIFY=true
UMOJA_OPENSEARCH_FAIL_CLOSED=true
UMOJA_OPENSEARCH_REDACTED_AUDIT_ONLY=true
