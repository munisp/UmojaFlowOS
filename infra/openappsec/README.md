# open-appsec boundary

open-appsec is disabled until a managed agent token, policy bundle, and approved ingress deployment are configured. It protects APISIX ingress but never replaces Keycloak identity validation, Permify authorization, payment policy, or provider controls.
