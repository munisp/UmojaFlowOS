# GeoLibre boundary

GeoLibre is disabled until an approved tile source and map style are configured. It may consume only the reporting service's aggregated jurisdiction endpoint; it must never receive raw coordinates, KYC/KYB evidence, account identifiers, or provider credentials.
